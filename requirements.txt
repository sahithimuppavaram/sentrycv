# Core CV + tracking
ultralytics>=8.2.0        # YOLOv8 detection + built-in ByteTrack
opencv-python>=4.9.0
numpy>=1.26
# Cross-camera re-identification
torch>=2.2
torchvision>=0.17
torchreid>=1.4.0          # OSNet re-ID embeddings  (pip install torchreid)
scipy>=1.12
# Serving + dashboard
fastapi>=0.110
uvicorn>=0.29
# Optional metrics
motmetrics>=1.4.0         # MOTA / IDF1 if you have ground truth
